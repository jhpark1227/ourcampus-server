package com.example.school.reservation.domain;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ReservationAlarmRepository extends JpaRepository<ReservationAlarm, Long> {
}
